<?xml version="1.0"?>
<psalm
    errorLevel="1"
    resolveFromConfigFile="true"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns="https://getpsalm.org/schema/config"
    xsi:schemaLocation="https://getpsalm.org/schema/config vendor/vimeo/psalm/config.xsd"
    findUnusedBaselineEntry="true"
    findUnusedCode="true"
	phpVersion="8.2"
    errorBaseline="psalm-baseline.xml"
>
    <projectFiles>
        <directory name="lib" />
        <ignoreFiles>
            <directory name="vendor" />
        </ignoreFiles>
    </projectFiles>
	<extraFiles>
		<directory name="vendor"/>
	</extraFiles>
	<stubs>
		<!-- Runtime-only `oidc` app classes (not a Composer dependency). -->
		<file name="tests/psalm-stubs/oidc-events.phpstub"/>
		<!-- Server-internal token provider (no public API for admin provisioning). -->
		<file name="tests/psalm-stubs/oc-token-provider.phpstub"/>
		<!-- Server-internal hook interface that OCP\Files\IRootFolder extends. -->
		<file name="tests/psalm-stubs/oc-hooks-emitter.phpstub"/>
	</stubs>
</psalm>
