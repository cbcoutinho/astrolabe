# E2E integration tests for Astrolabe frontend
#
# Runs Playwright tests against a real Nextcloud + MCP server environment
# using docker-compose. The MCP server runs in login-flow mode.
#
# SPDX-FileCopyrightText: 2025 Astrolabe contributors
# SPDX-License-Identifier: MIT

name: E2E Tests

on:
  pull_request:
  workflow_dispatch:

# Permissions are declared per-job (GitHub Actions least-privilege: scope the
# token to each job rather than the whole workflow — githubactions:S8264).
concurrency:
  group: e2e-${{ github.head_ref || github.run_id }}
  cancel-in-progress: true

jobs:
  e2e:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    name: e2e (${{ matrix.lane }})
    permissions:
      contents: read

    strategy:
      fail-fast: false
      matrix:
        include:
          # Nextcloud is the IdP (`oidc` app).
          - lane: internal-idp
            config: ''
            compose: '-f tests/e2e/docker-compose.yml'
          # Nextcloud is a client of Keycloak (`user_oidc` app) — GH #324.
          # Gating, as of MCP server 0.176.3: before that the server read only
          # the `client_id` claim, so it refused the Keycloak tokens (which
          # carry `azp`) this lane presents.
          - lane: external-idp
            config: '--config playwright.external-idp.config.ts'
            compose: '-f tests/e2e/docker-compose.yml -f tests/e2e/docker-compose.external-idp.yml'

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Read package.json node and npm engines version
        uses: skjnldsv/read-package-engines-version-actions@06d6baf7d8f41934ab630e97d9e6c0bc9c9ac5e4 # v3
        id: versions
        with:
          fallbackNode: '^22'
          fallbackNpm: '^10'

      - name: Set up node ${{ steps.versions.outputs.nodeVersion }}
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: ${{ steps.versions.outputs.nodeVersion }}

      - name: Set up npm ${{ steps.versions.outputs.npmVersion }}
        # `--ignore-scripts`: installing npm itself has no lifecycle scripts we
        # want to run. The version is an engines range rather than a pin because
        # it is read from `package.json`, this project's single source of truth
        # for it (see ci.yml's `package` job).
        run: npm i -g --ignore-scripts 'npm@${{ steps.versions.outputs.npmVersion }}' # NOSONAR (githubactions:S8543)

      - name: Get php version
        id: php-versions
        uses: icewind1991/nextcloud-version-matrix@8a7bac6300b2f0f3100088b297995a229558ddba # v1.3.2
        with:
          filename: appinfo/info.xml

      - name: Set up PHP ${{ steps.php-versions.outputs.php-max }}
        uses: shivammathur/setup-php@f3e473d116dcccaddc5834248c87452386958240 # 2.37.2
        with:
          php-version: ${{ steps.php-versions.outputs.php-max }}
          coverage: none

      - name: Install dependencies & build app
        env:
          CYPRESS_INSTALL_BINARY: 0
          PUPPETEER_SKIP_DOWNLOAD: true
        run: |
          composer install --no-dev --optimize-autoloader
          npm ci --ignore-scripts
          npm run build

      - name: Get installed Playwright version
        id: playwright
        run: echo "version=$(node -e "console.log(require('@playwright/test/package.json').version)")" >> "$GITHUB_OUTPUT"

      - name: Cache Playwright browsers
        id: playwright-cache
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright-${{ steps.playwright.outputs.version }}

      # Install the OS-level libraries Playwright's browsers need. These are
      # not part of the cached ~/.cache/ms-playwright dir, so this runs on
      # every job; apt is reliable, so no retry is needed here.
      - name: Install Playwright OS dependencies
        timeout-minutes: 8
        # `--no-install`: run the Playwright pinned in package-lock.json, never
        # fetch one on demand (githubactions:S6505/S8543).
        run: npx --no-install playwright install-deps chromium

      # Provision Chromium WITHOUT the Playwright CDN. The cdn.playwright.dev
      # download deterministically stalls at "100%" on GitHub's runners (it never
      # finalizes — confirmed over multiple runs), so instead we copy the browsers
      # out of the official Playwright container image, which is served from
      # mcr.microsoft.com (a container registry, unaffected by the CDN issue).
      # Runs only on a cache miss; on a hit the cached browsers under
      # ~/.cache/ms-playwright are reused. The image tag matches the installed
      # @playwright/test version, so the Chromium build is guaranteed to match.
      - name: Provision Playwright browsers from official image
        if: steps.playwright-cache.outputs.cache-hit != 'true'
        timeout-minutes: 10
        run: |
          image="mcr.microsoft.com/playwright:v${{ steps.playwright.outputs.version }}-noble"
          echo "Pulling ${image}"
          docker pull "${image}"
          mkdir -p ~/.cache/ms-playwright
          cid=$(docker create "${image}")
          # The official image stores browsers at /ms-playwright; copy them into
          # the default cache location Playwright looks in on the host runner.
          docker cp "${cid}:/ms-playwright/." ~/.cache/ms-playwright/
          docker rm "${cid}"

      - name: Verify Chromium is present (no CDN download)
        run: |
          if ! ls -d ~/.cache/ms-playwright/chromium-* >/dev/null 2>&1; then
            echo "::error::Chromium was not provisioned into ~/.cache/ms-playwright"
            ls -la ~/.cache/ms-playwright 2>/dev/null || true
            exit 1
          fi
          echo "Chromium present:"
          ls -d ~/.cache/ms-playwright/chromium-*

      - name: Run E2E tests
        timeout-minutes: 30
        run: npx --no-install playwright test ${{ matrix.config }}

      - name: Collect service logs
        if: always()
        run: |
          docker compose ${{ matrix.compose }} logs --tail=500 > /tmp/docker-compose-logs.txt 2>&1 || true

      - name: Tear down services
        if: always()
        run: |
          docker compose ${{ matrix.compose }} down -v --remove-orphans || true

      - name: Upload test results
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        if: always()
        with:
          name: e2e-report-${{ matrix.lane }}
          path: |
            test-results/
            playwright-report/
            /tmp/docker-compose-logs.txt
          retention-days: 7
          if-no-files-found: ignore

  summary:
    runs-on: ubuntu-latest
    permissions:
      contents: none
    needs: [e2e]
    if: always()
    name: e2e-summary
    steps:
      - name: Summary status
        run: |
          if [[ "${{ needs.e2e.result }}" != "success" && "${{ needs.e2e.result }}" != "skipped" ]]; then
            exit 1
          fi
